let start = parseInt(prompt("Please enter a number to start counting with: "));
const end = parseInt(prompt("Please enter a number to stop at: "));

while (start < end){
    start = (start + 1);
    console.log(start);
}

