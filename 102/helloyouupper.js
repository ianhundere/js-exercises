const name = prompt(`what is your name? `.toUpperCase());
const answer = `hello ` + name + ` ! `;
const letters = `your name has ${name.length} letters in it! awesome!`;

console.log(answer.toUpperCase() + letters.toUpperCase());
