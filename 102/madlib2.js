console.log(`Let's play a game of madlibs!`)

const name = prompt(`Choose a name: `)

const subject = prompt(`Choose a subject: `)

const madlib = `${name}'s favorite subject in school is ${subject}.`

console.log(madlib)